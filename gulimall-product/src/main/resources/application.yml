spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.100.133:3306/gulimall_pms?useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    nacos:
      discovery:
        # Nacos注册中心的地址
        server-addr: 192.168.100.133:8848
    alicloud:
      # 配置AccessKey
      access-key: LTAI5tEBjMBdeVSmkzpesNJh
      # 配置AccessKeySecret
      secret-key: a0Rx3CNeogfdLh7DxW3vzGfIkJjgDU
      oss:
        # 配置endpoint
        endpoint: oss-cn-beijing.aliyuncs.com


mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml  # 寻找mapper文件
  global-config:
    db-config:
      id-type: auto  # 主键自增
      # 逻辑已删除值(默认为 1)
      logic-delete-value: 1
      # 逻辑未删除值(默认为 0)
      logic-not-delete-value: 0
server:
  port: 10000
# 调整日志级别
logging:
  level:
    com.example.gulimall: debug
