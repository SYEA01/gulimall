spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.100.135:3306/gulimall_sms?useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    nacos:
      discovery:
        # nacos注册中心的地址
        server-addr: 192.168.100.135:8848
  application:
    # 给这个服务起个名字 （这样Nacos注册中心就能发现这个服务了）
    name: gulimall-coupon


mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml  # 寻找mapper文件
  global-config:
    db-config:
      id-type: auto  # 主键自增
server:
  port: 7000

